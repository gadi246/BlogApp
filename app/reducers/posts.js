const DEFAULT_STATE = { all: [], visiblePost:null };
import { FETCH_POSTS } from '../actions/action-creators';

export const posts = (state = DEFAULT_STATE, action) => {
   switch (action.type){

   }
  return state;
};
